@{
    ViewData["Title"] = "Vinam Purchase Invoice List";
    if (Context.Request.Headers["x-requested-with"] == "XMLHttpRequest")
    {
        Layout = null;
    }
    else
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-wrapper" style="background-color:#ffffff">
    <div class="col-md-6 mt-3 ms-3">
                    <h4 class="card-title pb-4">Vinam Purchase Invoice List</h4>
                    <hr />
                </div>
    <div class="card col-md-6">
        <form action="~/VinamPurchaseInvoiceList/VinamPurchaseInvoiceListExportExcel" method="post">
            <div class="card-body">
                <h4 class="card-title">Lọc theo ngày</h4>
                <label class="mt-3">From Date</label>
                <div class="input-group">
                    <input type="text" class="form-control datepicker-autoclose" id="fromDate" name="fromDate"
                           placeholder="mm/dd/yyyy" autocomplete="off">
                    <div class="input-group-append">
                        <span class="input-group-text h-100"><i class="fa fa-calendar"></i></span>
                    </div>
                </div>
                <label class="mt-3">To Date</label>
                <div class="input-group">
                    <input type="text" class="form-control datepicker-autoclose" id="toDate" name="toDate"
                           placeholder="mm/dd/yyyy" autocomplete="off">
                    <div class="input-group-append">
                        <span class="input-group-text h-100"><i class="fa fa-calendar"></i></span>
                    </div>
                </div>
                <div class="input-group">
                    <p class="text-danger mt-3">
                        @TempData["VinamPurchaseInvoiceListShow_Error"]
                        </>
                </div>
            </div>
            <div class="border-top">
                <div class="card-body">
                    <button type="submit" class="btn btn-md btn-success text-white invoiceLoc" style="padding-left:2em; padding-right:2em;"
                    id="export-button" name="export-button">Xuất Excel</button>
                    <!-- Xuất ra Báo cáo để tham khảo -->
                    <button type="submit" class="btn btn-md btn-success text-white invoiceLoc" style="padding-left:2em; padding-right:2em;"
                            id="export-excel-button" name="export-excel-button">
                        Xuất Excel TK
                    </button>
                </div>
            </div>
        </form>
        <!-- Hiệu ứng chờ -->
        <div id="loading-spinner">
            <div class="spinner"></div>
        </div>
    </div>
</div>
<link rel="stylesheet" type="text/css" href="~/assets/libs/bootstrap-datepicker/dist/css/bootstrap-datepicker.min.css">
<link rel="stylesheet" type="text/css" href="~/assets/libs/quill/dist/quill.snow.css">
<link rel="stylesheet" type="text/css" href="~/assets/libs/select2/dist/css/select2.min.css">
<link rel="stylesheet" type="text/css" href="~/assets/libs/jquery-minicolors/jquery.minicolors.css">
<link href="~/assets/libs/datatables/media/css/jquery.datatables.min.css" rel="stylesheet" />
<script src="~/assets/libs/bootstrap-datepicker/dist/js/bootstrap-datepicker.min.js"></script>
<script src="~/assets/libs/datatables/media/js/jquery.datatables.min.js"></script>
    <script>
        /*datwpicker*/
        jQuery('.datepicker-autoclose').datepicker({
            autoclose: true,
            todayHighlight: true,
            orientation: 'bottom'
        });
        // var quill = new Quill('#editor', {
        //     theme: 'snow'
        // });

    </script>
    <script>
        $(document).ready(function () {
            $("form").submit(function (e) {
                e.preventDefault(); // Ngan ch?n form g?i d? li?u m?t cách thông thu?ng

                var startDate = $("#fromDate").val();
                var endDate = $("#toDate").val();

                // Ki?m tra xem startDate và endDate dã du?c ch?n hay chua
                if (!startDate || !endDate) {
                    alert("Vui lòng ch?n c? ngày b?t d?u và ngày k?t thúc.");
                    return; // D?ng x? lý n?u không ch?n d? hai ngày
                }

                // Hi?n th? hi?u ?ng ch?
                $("#loading-spinner").show();
                // Vô hi?u hóa button
                $("#export-button").prop("disabled", true);

                // G?i d? li?u b?ng Ajax d?n controller
                $.ajax({
                    url: "/VinamPurchaseInvoiceList/VinamPurchaseInvoiceListExportExcel", // Ðu?ng d?n d?n action trong controller
                    type: "POST",
                    data: {
                        startDate: startDate,
                        endDate: endDate
                    },
                    xhrFields: {
                        responseType: 'blob'
                    },
                    success: function (data) {
                        var blob = new Blob([data], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
                        // ?n hi?u ?ng ch? sau khi nh?n k?t qu? t? controller
                        $("#loading-spinner").hide();
                        // Kích ho?t l?i button
                        $("#export-button").prop("disabled", false);

                        var link = document.createElement('a');
                        link.href = window.URL.createObjectURL(blob);
                        link.download = 'VinamPurchaseInvoiceList.xlsx'; // Ð?t tên file Excel
                        link.style.display = 'none';

                        document.body.appendChild(link);
                        link.click();

                        window.URL.revokeObjectURL(link.href);
                        document.body.removeChild(link);
                    }
                });
            });
        });
    </script>
    <script>
        $(document).ready(function () {
            // S? ki?n khi nút "Xu?t Excel M?i" du?c nh?p
            $("#export-excel-button").click(function () {
                var startDate = $("#fromDate").val();
                var endDate = $("#toDate").val();

                // Ki?m tra xem startDate và endDate dã du?c ch?n hay chua
                if (!startDate || !endDate) {
                    alert("Vui lòng ch?n c? ngày b?t d?u và ngày k?t thúc.");
                    return; // D?ng x? lý n?u không ch?n d? hai ngày
                }

                // Hi?n th? hi?u ?ng ch?
                $("#loading-spinner").show();
                // Vô hi?u hóa button
                $("#export-button").prop("disabled", true);
                // Vô hi?u hóa nút "Xu?t Excel M?i"
                $("#export-excel-button").prop("disabled", true);

                // G?i d? li?u b?ng Ajax d?n controller
                $.ajax({
                    url: "/VinamPurchaseInvoiceList/VinamPurchaseInvoiceListExportExcelTK", // Ðu?ng d?n d?n action trong controller
                    type: "POST",
                    data: {
                        startDate: startDate,
                        endDate: endDate
                    },
                    xhrFields: {
                        responseType: 'blob'
                    },
                    success: function (data) {
                        var blob = new Blob([data], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
                        // ?n hi?u ?ng ch? sau khi nh?n k?t qu? t? controller
                        $("#loading-spinner").hide();
                        // Kích ho?t l?i button
                        $("#export-button").prop("disabled", false);
                        // Kích ho?t l?i nút "Xu?t Excel M?i"
                        $("#export-excel-button").prop("disabled", false);
                        var link = document.createElement('a');
                        link.href = window.URL.createObjectURL(blob);
                        link.download = 'VinamPurchaseInvoiceListTK.xlsx'; // Ð?t tên file Excel
                        link.style.display = 'none';

                        document.body.appendChild(link);
                        link.click();

                        window.URL.revokeObjectURL(link.href);
                        document.body.removeChild(link);
                    }
                });
            });
        });
    </script>

@section css{
    <link rel="stylesheet" type="text/css"
          href="~/assets/libs/bootstrap-datepicker/dist/css/bootstrap-datepicker.min.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/quill/dist/quill.snow.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/select2/dist/css/select2.min.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/jquery-minicolors/jquery.minicolors.css">
    <link href="~/css/spinner.css" rel="stylesheet" />
    }

@section script{
    <link rel="stylesheet" type="text/css" href="~/assets/libs/bootstrap-datepicker/dist/css/bootstrap-datepicker.min.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/quill/dist/quill.snow.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/select2/dist/css/select2.min.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/jquery-minicolors/jquery.minicolors.css">
    <link href="~/assets/libs/datatables/media/css/jquery.datatables.min.css" rel="stylesheet" />
    <script src="~/assets/libs/bootstrap-datepicker/dist/js/bootstrap-datepicker.min.js"></script>
    <script src="~/assets/libs/datatables/media/js/jquery.datatables.min.js"></script>

    <script>
        /*datwpicker*/
        jQuery('.datepicker-autoclose').datepicker({
            autoclose: true,
            todayHighlight: true,
            orientation: 'bottom'
        });
        // var quill = new Quill('#editor', {
        //     theme: 'snow'
        // });

    </script>
    <script>
        $(document).ready(function () {
            $("form").submit(function (e) {
                e.preventDefault(); // Ngăn chặn form gửi dữ liệu một cách thông thường

                var startDate = $("#fromDate").val();
                var endDate = $("#toDate").val();

                // Kiểm tra xem startDate và endDate đã được chọn hay chưa
                if (!startDate || !endDate) {
                    alert("Vui lòng chọn cả ngày bắt đầu và ngày kết thúc.");
                    return; // Dừng xử lý nếu không chọn đủ hai ngày
                }

                // Hiển thị hiệu ứng chờ
                $("#loading-spinner").show();
                // Vô hiệu hóa button
                $("#export-button").prop("disabled", true);

                // Gửi dữ liệu bằng Ajax đến controller
                $.ajax({
                    url: "/VinamPurchaseInvoiceList/VinamPurchaseInvoiceListExportExcel", // Đường dẫn đến action trong controller
                    type: "POST",
                    data: {
                        startDate: startDate,
                        endDate: endDate
                    },
                    xhrFields: {
                        responseType: 'blob'
                    },
                    success: function (data) {
                        var blob = new Blob([data], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
                        // Ẩn hiệu ứng chờ sau khi nhận kết quả từ controller
                        $("#loading-spinner").hide();
                        // Kích hoạt lại button
                        $("#export-button").prop("disabled", false);

                        var link = document.createElement('a');
                        link.href = window.URL.createObjectURL(blob);
                        link.download = 'VinamPurchaseInvoiceList.xlsx'; // Đặt tên file Excel
                        link.style.display = 'none';

                        document.body.appendChild(link);
                        link.click();

                        window.URL.revokeObjectURL(link.href);
                        document.body.removeChild(link);
                    }
                });
            });
        });
    </script>
    <script>
        $(document).ready(function () {
            // Sự kiện khi nút "Xuất Excel Mới" được nhấp
            $("#export-excel-button").click(function () {
                var startDate = $("#fromDate").val();
                var endDate = $("#toDate").val();

                // Kiểm tra xem startDate và endDate đã được chọn hay chưa
                if (!startDate || !endDate) {
                    alert("Vui lòng chọn cả ngày bắt đầu và ngày kết thúc.");
                    return; // Dừng xử lý nếu không chọn đủ hai ngày
                }

                // Hiển thị hiệu ứng chờ
                $("#loading-spinner").show();
                // Vô hiệu hóa button
                $("#export-button").prop("disabled", true);
                // Vô hiệu hóa nút "Xuất Excel Mới"
                $("#export-excel-button").prop("disabled", true);

                // Gửi dữ liệu bằng Ajax đến controller
                $.ajax({
                    url: "/VinamPurchaseInvoiceList/VinamPurchaseInvoiceListExportExcelTK", // Đường dẫn đến action trong controller
                    type: "POST",
                    data: {
                        startDate: startDate,
                        endDate: endDate
                    },
                    xhrFields: {
                        responseType: 'blob'
                    },
                    success: function (data) {
                        var blob = new Blob([data], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
                        // Ẩn hiệu ứng chờ sau khi nhận kết quả từ controller
                        $("#loading-spinner").hide();
                        // Kích hoạt lại button
                        $("#export-button").prop("disabled", false);
                        // Kích hoạt lại nút "Xuất Excel Mới"
                        $("#export-excel-button").prop("disabled", false);
                        var link = document.createElement('a');
                        link.href = window.URL.createObjectURL(blob);
                        link.download = 'VinamPurchaseInvoiceListTK.xlsx'; // Đặt tên file Excel
                        link.style.display = 'none';

                        document.body.appendChild(link);
                        link.click();

                        window.URL.revokeObjectURL(link.href);
                        document.body.removeChild(link);
                    }
                });
            });
        });
    </script>
    }
