@using System.Data;
@using System.Globalization;
@{
    ViewData["Title"] = "VINAM AU REPORT";
    Layout = "~/Views/Shared/_EmptyLayout.cshtml";
    @model System.Data.DataTable

}

<!DOCTYPE html>
<html dir="ltr" lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- Tell the browser to be responsive to screen width -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="keywords"
          content="wrappixel, admin dashboard, html css dashboard, web dashboard, bootstrap 5 admin, bootstrap 5, css3 dashboard, bootstrap 5 dashboard, Matrix lite admin bootstrap 5 dashboard, frontend, responsive bootstrap 5 admin template, Matrix admin lite design, Matrix admin lite dashboard bootstrap 5 dashboard template">
    <meta name="description"
          content="Matrix Admin Lite Free Version is powerful and clean admin dashboard template, inpired from Bootstrap Framework">
    <meta name="robots" content="noindex,nofollow">
    <!-- Favicon icon -->
    <link rel="icon" type="image/png" sizes="16x16" href="../../assets/images/favicon.png">
    <!-- Custom CSS -->
    <link rel="stylesheet" type="text/css" href="../../assets/extra-libs/multicheck/multicheck.css">
    <link href="../../assets/libs/datatables.net-bs4/css/dataTables.bootstrap4.css" rel="stylesheet">
    <link href="../../dist/css/style.min.css" rel="stylesheet">
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    <style>
        .card-body {
            padding: 0 !important;
        }
    </style>
</head>

<body>
    <div id="main-wrapper" data-layout="vertical" data-navbarbg="skin5" data-sidebartype="full"
         data-sidebar-position="absolute" data-header-position="absolute" data-boxed-layout="full">
        <div class="page-wrapper" style="margin-left:0px">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-3">
                        <div>
                            <h4 class="text-center mb-3 pb-2 pt-2" style="color:red; background-color:white">Payroll Date : <span style="color:blue !important">@DateTime.Now.ToString("dd/MM/yyyy")</span> </h4>
                        </div>
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h3 class="card-title" style="color:red">BTA</h3>
                                </div>
                                <div class="table-container">
                                    <div class="table-responsive">
                                        <table id="zero_config" class="table table-striped table-bordered">
                                            <thead style="font-weight:bold !important">
                                                <tr>
                                                    <th>Employee Name</th>
                                                    <th>I</th>
                                                    <th>P</th>
                                                    <th>S</th>
                                                    <th>Grand Total</th>
                                                </tr>
                                            </thead>
                                            <tbody class="dataTables_scroll">
                                                @foreach (DataRow row in Model.Rows)
                                                {
                                                    if (row["Address"].ToString() == "BTA")
                                                    {
                                                        <tr>
                                                            @if (Convert.ToInt32(row["stt"]) == 1)
                                                            {
                                                                <td style="font-weight:bold">@row["Name"]</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else if (Convert.ToInt32(row["stt"]) == 2)
                                                            {
                                                                <td style="padding-left:2em">@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else
                                                            {
                                                                <td>@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                        </tr>
                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>

                                </div>

                            </div>
                        </div>
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h3 class="card-title" style="color:red">EDM</h3>
                                </div>
                                <div class="table-container">
                                    <div class="table-responsive">
                                        <table id="zero_config" class="table table-striped table-bordered">
                                            <thead style="font-weight:bold !important">
                                                <tr>
                                                    <th>Employee Name</th>
                                                    <th>I</th>
                                                    <th>P</th>
                                                    <th>S</th>
                                                    <th>Grand Total</th>
                                                </tr>
                                            </thead>
                                            <tbody class="dataTables_scroll">
                                                @foreach (DataRow row in Model.Rows)
                                                {
                                                    if (row["Address"].ToString() == "EDM")
                                                    {
                                                        <tr>
                                                            @if (Convert.ToInt32(row["stt"]) == 1)
                                                            {
                                                                <td style="font-weight:bold">@row["Name"]</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else if (Convert.ToInt32(row["stt"]) == 2)
                                                            {
                                                                <td style="padding-left:2em">@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else
                                                            {
                                                                <td>@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                        </tr>
                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>

                                </div>

                            </div>
                        </div>
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h3 class="card-title" style="color:red">MAL</h3>
                                </div>
                                <div class="table-container">
                                    <div class="table-responsive">
                                        <table id="zero_config" class="table table-striped table-bordered">
                                            <thead style="font-weight:bold !important">
                                                <tr>
                                                    <th>Employee Name</th>
                                                    <th>I</th>
                                                    <th>P</th>
                                                    <th>S</th>
                                                    <th>Grand Total</th>
                                                </tr>
                                            </thead>
                                            <tbody class="dataTables_scroll">
                                                @foreach (DataRow row in Model.Rows)
                                                {
                                                    if (row["Address"].ToString() == "MAL")
                                                    {
                                                        <tr>
                                                            @if (Convert.ToInt32(row["stt"]) == 1)
                                                            {
                                                                <td style="font-weight:bold">@row["Name"]</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else if (Convert.ToInt32(row["stt"]) == 2)
                                                            {
                                                                <td style="padding-left:2em">@row["Name"]</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else
                                                            {
                                                                <td>@row["Name"]</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}%", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                        </tr>
                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>

                                </div>

                            </div>
                        </div>
                    </div>
                    <div class="col-3">
                        <div>
                            <h4 class="text-center mb-3 pb-2 pt-2" style="color:red; background-color:white">AU : <span style="color:blue !important">@ViewBag.au%</span> </h4>
                        </div>
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h3 class="card-title" style="color:red">WED</h3>
                                </div>
                                <div class="table-container">
                                    <div class="table-responsive">
                                        <table id="zero_config" class="table table-striped table-bordered">
                                            <thead style="font-weight:bold !important">
                                                <tr>
                                                    <th>Employee Name</th>
                                                    <th>I</th>
                                                    <th>P</th>
                                                    <th>S</th>
                                                    <th>Grand Total</th>
                                                </tr>
                                            </thead>
                                            <tbody class="dataTables_scroll">
                                                @foreach (DataRow row in Model.Rows)
                                                {
                                                    if (row["Address"].ToString() == "WED")
                                                    {
                                                        <tr>
                                                            @if (Convert.ToInt32(row["stt"]) == 1)
                                                            {
                                                                <td style="font-weight:bold">@row["Name"]</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else if (Convert.ToInt32(row["stt"]) == 2)
                                                            {
                                                                <td style="padding-left:2em">@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else
                                                            {
                                                                <td>@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                        </tr>
                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>

                                </div>

                            </div>
                        </div>
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h3 class="card-title" style="color:red">5MI</h3>
                                </div>
                                <div class="table-container">
                                    <div class="table-responsive">
                                        <table id="zero_config" class="table table-striped table-bordered">
                                            <thead style="font-weight:bold !important">
                                                <tr>
                                                    <th>Employee Name</th>
                                                    <th>I</th>
                                                    <th>P</th>
                                                    <th>S</th>
                                                    <th>Grand Total</th>
                                                </tr>
                                            </thead>
                                            <tbody class="dataTables_scroll">
                                                @foreach (DataRow row in Model.Rows)
                                                {
                                                    if (row["Address"].ToString() == "5MI")
                                                    {
                                                        <tr>
                                                            @if (Convert.ToInt32(row["stt"]) == 1)
                                                            {
                                                                <td style="font-weight:bold">@row["Name"]</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else if (Convert.ToInt32(row["stt"]) == 2)
                                                            {
                                                                <td style="padding-left:2em">@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else
                                                            {
                                                                <td>@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                        </tr>
                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>

                                </div>

                            </div>
                        </div>
                    </div>
                    <div class="col-3">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h3 class="card-title" style="color:red">LTH</h3>
                                </div>
                                <div class="table-container">
                                    <div class="table-responsive">
                                        <table id="zero_configLTH" class="table table-striped table-bordered">
                                            <thead style="font-weight:bold !important">
                                                <tr>
                                                    <th>Employee Name</th>
                                                    <th>I</th>
                                                    <th>P</th>
                                                    <th>S</th>
                                                    <th>Grand Total</th>
                                                </tr>
                                            </thead>
                                            <tbody class="dataTables_scroll">
                                                @foreach (DataRow row in Model.Rows)
                                                {
                                                    if (row["Address"].ToString() == "LTH")
                                                    {
                                                        <tr>
                                                            @if (Convert.ToInt32(row["stt"]) == 1)
                                                            {
                                                                <td style="font-weight:bold">@row["Name"]</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else if (Convert.ToInt32(row["stt"]) == 2)
                                                            {
                                                                <td style="padding-left:2em">@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else
                                                            {
                                                                <td>@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                        </tr>
                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>

                                </div>

                            </div>
                        </div>
                    </div>
                    <div class="col-3">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center justify-content-between">
                                    <h3 class="card-title" style="color:red">MIL</h3>
                                </div>
                                <div class="table-container">
                                    <div class="table-responsive">
                                        <table id="zero_configMIL" class="table table-striped table-bordered">
                                            <thead style="font-weight:bold !important">
                                                <tr>
                                                    <th>Employee Name</th>
                                                    <th>I</th>
                                                    <th>P</th>
                                                    <th>S</th>
                                                    <th>Grand Total</th>
                                                </tr>
                                            </thead>
                                            <tbody class="dataTables_scroll">
                                                @foreach (DataRow row in Model.Rows)
                                                {
                                                    if (row["Address"].ToString() == "MIL")
                                                    {
                                                        <tr>
                                                            @if (Convert.ToInt32(row["stt"]) == 1)
                                                            {
                                                                <td style="font-weight:bold">@row["Name"]</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td style="font-weight:bold">@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else if (Convert.ToInt32(row["stt"]) == 2)
                                                            {
                                                                <td style="padding-left:2em">@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                            else
                                                            {
                                                                <td>@row["Name"]</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["I"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["P"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["S"]),2))</td>
                                                                <td>@string.Format("{0}", Math.Round(Convert.ToDecimal(row["GrandTotal"]),2))</td>
                                                            }
                                                        </tr>

                                                    }
                                                }
                                            </tbody>
                                        </table>
                                    </div>

                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <link rel="stylesheet" type="text/css"
          href="~/assets/libs/bootstrap-datepicker/dist/css/bootstrap-datepicker.min.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/quill/dist/quill.snow.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/select2/dist/css/select2.min.css">
    <link rel="stylesheet" type="text/css" href="~/assets/libs/jquery-minicolors/jquery.minicolors.css">
    <link href="~/css/spinner.css" rel="stylesheet" />
    <script src="../../assets/libs/jquery/dist/jquery.min.js"></script>
    <!-- Bootstrap tether Core JavaScript -->
    <script src="../../assets/libs/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <!-- slimscrollbar scrollbar JavaScript -->
    <script src="../../assets/libs/perfect-scrollbar/dist/perfect-scrollbar.jquery.min.js"></script>
    <script src="../../assets/extra-libs/sparkline/sparkline.js"></script>
    <!--Wave Effects -->
    <script src="../../dist/js/waves.js"></script>
    <!--Menu sidebar -->
    <script src="../../dist/js/sidebarmenu.js"></script>
    <!--Custom JavaScript -->
    <script src="../../dist/js/custom.min.js"></script>
    <!-- this page js -->
    <script src="../../assets/extra-libs/multicheck/datatable-checkbox-init.js"></script>
    <script src="../../assets/extra-libs/multicheck/jquery.multicheck.js"></script>
    <script src="../../assets/extra-libs/DataTables/datatables.min.js"></script>
    <script src="~/assets/libs/bootstrap-datepicker/dist/js/bootstrap-datepicker.min.js"></script>
    <script src="https://cdn.datatables.net/fixedheader/3.2.4/js/dataTables.fixedHeader.min.js"></script>
    <script>
        $(document).ready(function () {
            var table = $('#zero_configLTH').DataTable({
                paging: true, // Enable paging
                searching: false, // Disable search
                lengthChange: false, // Disable length change
                ordering: false, // Disable automatic ordering
                "pageLength": 8 // Set the page length to 5 rows
            });

            var table1 = $('#zero_configMIL').DataTable({
                paging: true, // Enable paging
                searching: false, // Disable search
                lengthChange: false, // Disable length change
                ordering: false, // Disable automatic ordering
                "pageLength": 8 // Set the page length to 5 rows
            });

            setInterval(function () {
                var info = table.page.info();
                var nextPage = (info.page + 1) % info.pages; // Calculate the next page index
                table.page(nextPage).draw('page'); // Go to the next page
            }, 10000); // 5000 milliseconds = 5 seconds

            setInterval(function () {
                var info = table1.page.info();
                var nextPage = (info.page + 1) % info.pages; // Calculate the next page index
                table1.page(nextPage).draw('page'); // Go to the next page
            }, 10000); // 5000 milliseconds = 5 seconds

            // Hàm reload trang sau mỗi 5 phút (300000 milliseconds)
            // setTimeout(function () {
            //     location.reload();
            // }, 300000); // 300000 milliseconds = 5 minutes
            setTimeout(function () {
                window.location.href = "http://172.16.142.100:8085/EFFReport";
            }, 300000);

        });
    </script>
</body>

</html>